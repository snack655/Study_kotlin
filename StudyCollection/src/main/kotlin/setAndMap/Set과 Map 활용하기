Set과 Map 활용하기

Set은 정해진 순서가 없는 요소들의 집합을 나타내는 컬렉션이다.
List의 경우 값이 중복되더라도 요소로 저장할 수 있었지만
Set의 경우에는 집합의 개념이기 때문에 동일한 요소를 중복해서 가질 수 없다.
다시 말하면 모든 요소가 유일(unique)해야 한다.
Map은 요소가 키와 값의 쌍의 형태로 저장된다.
키는 중복될 수 없고 유일하다.
하지만 값은 중복해서 사용할 수 있다.