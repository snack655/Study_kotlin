package list.createImmutableList

/**
 * emptyList() 함수
 *
 * 비어 있는 List를 생성하려면 emptyList<>()를 사용할 수 있다.
 * 이때는 반드시 형식 매개변수를 지정한다.
 */

fun main() {
    var emptyList: List<String> = emptyList<String>()
}