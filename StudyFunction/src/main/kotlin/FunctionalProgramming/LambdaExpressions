람다식(Lambda Expressions)
람다식은 람다 대수(Lambda Calculus)에서 유래한 것
아래와 같은 형식이다.
{ x, y -> x + y }    람다식의 예(이름이 없는 함수 형태)

위의 식을 보면 함수의 이름이 없고 화살표(->)가 사용되었다.
함수형 프로그래밍의 람다식은 다른 함수의 인자로 넘기는 함수,
함수의 결과값으로 반환하는 함수, 변수에 저장하는 함수를 말한다.
